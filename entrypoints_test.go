package main

import (
	"testing"
)

func Test_get_possible_entrypoints(t *testing.T) {
	/*
		// 1

		afps, fset, err := parse_dir_afps("test_files/multifile")

		if err != nil {
			t.Fatalf("Parsing dir test_files/recursion should not throw an error.\n")
		}

		fmt.Println(get_possible_entrypoints(fset, afps))

		// 2

		afps, fset, err = parse_dir_afps(".")

		if err != nil {
			t.Fatalf("Parsing dir test_files/recursion should not throw an error.\n")
		}

		fmt.Println(get_possible_entrypoints(fset, afps))
	*/
}
